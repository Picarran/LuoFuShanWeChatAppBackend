server:
  port: 8001

spring:
  application:
    name: luofushan
  datasource:
    url: jdbc:mysql://localhost:3306/LuoFuShan?characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  data:
    redis:
      host: 127.0.0.1
      port: 6379
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 150MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


wechat:
  appid: 
  secret: 

auth:
  tokenTtlDays: 30

file:
  upload-dir: /home/luofushan/uploads
  url-prefix: https://www.picarran.xyz:8002/uploads