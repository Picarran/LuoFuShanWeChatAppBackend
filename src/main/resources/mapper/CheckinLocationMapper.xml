<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.luofushan.dao.mapper.CheckinLocationMapper">

    <!-- 今日打卡数 + 1 -->
    <update id="incrementTodayCount">
        UPDATE checkin_location
        SET today_has_checkin = today_has_checkin + 1
        WHERE id = #{id}
    </update>

    <!-- 查询今日打卡数 -->
    <select id="selectTodayCount" resultType="java.lang.Long">
        SELECT today_has_checkin FROM checkin_location WHERE id = #{id}
    </select>

</mapper>
